##3. 두 실수를 배열에 초기화해서 저장하고 더한 후 화면에 출력 하는 C프로그램을 작성하시오.
```#include <stdio.h>
int main() {
    float arr[2] = {3.5, 2.5};
    float sum;
    sum = arr[0] + arr[1];
    printf("%f\n", sum);
    return 0;
}
```
